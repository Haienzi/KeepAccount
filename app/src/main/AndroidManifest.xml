<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.qiu.keepaccount">

    <application
        android:name=".KeepAccount"
        android:allowBackup="true"
        android:icon="@drawable/ic_bookmark"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.databackup.recover.RecoverActivity">
        </activity>
        <activity android:name=".mvp.databackup.backup.BackUpActivity">
        </activity>
        <activity android:name=".mvp.bookaccount.BookAccountActivity">
        </activity>
        <activity android:name=".mvp.account.AccountInfoActivity">
        </activity>
        <activity android:name=".mvp.books.AddBookActivity">
        </activity>
        <activity android:name=".mvp.books.BookActivity">
        </activity>
        <activity
            android:name=".ui.activity.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>